Create table R (
                      a INT,
                      b INT
) WITH (
      'path' = 'examples/data/r.dat'
);

Create table S (
                      b INT,
                      c INT
) WITH (
      'path' = 'examples/data/s.dat'
);

Create table T (
                      c INT,
                      d INT,
                      e INT,
                      f INT
) WITH (
      'path' = 'examples/data/t.dat'
);